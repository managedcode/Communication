<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <IsPackable>true</IsPackable>
        <DebugType>embedded</DebugType>
    </PropertyGroup>

    <PropertyGroup>
        <Title>ManagedCode.Communication.CQRS.AspNetCore</Title>
        <PackageId>ManagedCode.Communication.CQRS.AspNetCore</PackageId>
        <Description>ASP.NET Core transport for ManagedCode.Communication.CQRS: turns IAsyncEnumerable&lt;CqrsStreamChunk&lt;,&gt;&gt; returned from Minimal API endpoints and MVC actions into a Server-Sent Events response.</Description>
        <PackageTags>managedcode;communication;cqrs;sse;streaming;aspnetcore;result-pattern</PackageTags>
    </PropertyGroup>

    <ItemGroup>
        <FrameworkReference Include="Microsoft.AspNetCore.App" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\ManagedCode.Communication\ManagedCode.Communication.csproj" />
        <ProjectReference Include="..\ManagedCode.Communication.CQRS\ManagedCode.Communication.CQRS.csproj" />
    </ItemGroup>

    <ItemGroup>
        <InternalsVisibleTo Include="ManagedCode.Communication.Tests" />
    </ItemGroup>

</Project>
